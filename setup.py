from setuptools import setup

setup(
    name="mybook",
    version="0.1",
    install_requires=[
        "sphinx",
        "sphinx-book-theme",
        "jupyter-sphinx",
        "sphinx_togglebutton",
        "sphinx_copybutton",
        "myst_nb",
        "jupyter-book",
    ],
)
